<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ZoesAndroidLabs" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/my_primary</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/my_secondary</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/my_statusbar</item>

        <!-- Text color. -->
        <item name="android:textColor">@color/my_text</item>

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ZoesAndroidLabs" parent="Base.Theme.ZoesAndroidLabs" />
</resources>